const lista = [1,2,3]

/*const MostrarLista=(datos)=>{
    if(datos.length === 0){
        console.log('lista vacia')}
    else{
        datos.forEach(element =>{
            console.log(element)
        })
    }
}
MostrarLista(lista)
MostrarLista([])*/

/*(function () {
    if(lista.length === 0){
        console.log('lista vacia')}
    else{
        console.table(lista)
    }
  })()*/

 
  const array1 = []
  console.log(array1.length)

  const array2 = new Array()
  console.log(array2.length)

   //newArray.push({nombre:'lala'})
  //newArray.push({nombre:'nestor'})
  //console.table(newArray)